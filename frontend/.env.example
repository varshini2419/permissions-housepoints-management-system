# Production / preview: backend origin (no trailing slash required; /api is appended for API calls).
# Vercel: add the same name + value in Project → Settings → Environment Variables.
VITE_API_URL=https://permissions-housepoints-management.onrender.com
